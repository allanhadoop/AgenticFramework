[project]
name = "agenticframework-deploy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Alan", email = "allanhadoop@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "modal>=1.5.5",
]

[project.scripts]
agenticframework-deploy = "agenticframework_deploy:main"

[build-system]
requires = ["uv_build>=0.12.4,<0.13.0"]
build-backend = "uv_build"
